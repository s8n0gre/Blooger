const fs = require('fs');

module.exports = {
  404: response => {
    response.writeHead(404);
    return response.end();
  },
  200: (response, asset) => {
    response.writeHead(200);
    return fs.createReadStream(asset)
      .pipe(response)
      .on('finish', response.end);
  },
};
